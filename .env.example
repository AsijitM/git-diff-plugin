# GitHub API Token (required for the --commit option)
GITHUB_TOKEN=your_github_token_here

# Repository information (optional, defaults to GitHub Actions environment variables if available)
REPO_OWNER=AsijitM
REPO_NAME=o11y-metadata
BRANCH=main

# CI-specific configuration (usually not needed to be set manually in CI environments)
# CI=true                    # Set to 'true' if running in a CI environment
# GITHUB_ACTIONS=true        # Automatically set in GitHub Actions
# GITHUB_WORKSPACE=/path     # Set to the workspace path in GitHub Actions
# GITHUB_REPOSITORY=owner/repo # The repository name including owner
# GITHUB_EVENT_NAME=push     # The event that triggered the workflow (push, pull_request, etc.)
